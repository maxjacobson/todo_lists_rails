<h1>Listing lists</h1>

<% @lists.each do |list| %>
  
    <h2><%= link_to list.name, list %> <%= "✓" if list.completed %> </h2>
    <p><strong>Created: </strong><%= list.created_at.strftime("%B %e, %Y - %l:%M %p") %></p>
    <p><strong>Last updated:</strong> <%= list.updated_at.strftime("%B %e, %Y - %l:%M %p") %></p>

    <ol>
    <% list.items.each do |item| %>
      <li>
        <%= item.name %>
        <%= "✓" if item.completed %>
      </li>

    <% end %>
    </ol>

    <%= link_to 'Edit', edit_list_path(list) %>
    <%= link_to 'Destroy', list, method: :delete, data: { confirm: 'Are you sure?' } %>

<% end %>


<br />

<%= link_to 'New List', new_list_path %>
